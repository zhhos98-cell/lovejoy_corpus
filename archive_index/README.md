# `archive_index/` — active archive routing layer

Status: **CURATED ACTIVE INDEX / HISTORICAL SEARCHES FROZEN**

This directory is no longer a flat log of every archive sweep. Use `ARCHIVE_ROUTER.md` first.

Five canonical controls remain at the directory root because living syntheses cite those stable paths directly. Supporting controls are grouped by institution or function under `aos_context/`, `jhu/`, `harvard/`, `lovejoy_targets/`, and `correspondence/`.

The complete former flat archive-index tree is preserved verbatim at `_frozen/snapshot_2026-09-05/`. Historical batch CSVs, handoffs, global repository sweeps, negative searches, and superseded acquisition targets are provenance only and should not be traversed by default.

Authority boundary:

`archive_index/ = locator / source identity / acquisition and custody provenance`

`archive_transcriptions/ = manuscript text / direct-image control`

`research_notes/ terminal syntheses = claim-level historical synthesis`

Default workflow:

> **terminal synthesis -> ARCHIVE_ROUTER -> exact active control -> frozen snapshot only when a specific provenance question requires it**
